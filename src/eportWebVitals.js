// src/reportWebVitals.js
const reportWebVitals = (metric) => {
    console.log(metric); // Par défaut, on affiche simplement les métriques dans la console
  };
  
  export default reportWebVitals;
  